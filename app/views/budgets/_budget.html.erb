<div id="<%= dom_id budget %>">
  <p>
    <strong>Name:</strong>
    <%= budget.name %>
  </p>

  <p>
    <strong>Amount:</strong>
    <%= budget.amount %>
  </p>

  <%= button_to "REMOVE", group_budget_path(@group, budget), method: :delete %>
</div>
