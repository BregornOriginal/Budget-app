<div id="<%= dom_id group %>">
  
  <p>
    <strong><%= link_to group.name, group_budgets_path(group.id) %></strong>
  </p>

  <p>
    <strong>Icon:</strong>
    <%= group.icon %>
  </p>
  <p>$ <%= group.total_price%></p>

</div>
